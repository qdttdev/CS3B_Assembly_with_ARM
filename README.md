# CS3B_Assembly_with_ARM

* These projects are implemented using Geany, run on Raspberry Pi and can be debugged using GDB.
* Refer to assignmentName_command.txt (if exist) to assemble and run the program.
